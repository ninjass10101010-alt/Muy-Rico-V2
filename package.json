{
  "name": "muy-rico",
  "private": true,
  "scripts": {
    "build": "cd home-bakery-management-system && npm install && npm run build"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
